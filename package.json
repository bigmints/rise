{
  "name": "rise",
  "private": true,
  "license": "MIT",
  "version": "1.0.0",
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "node scripts/dev.mjs --port 5173",
    "build": "npm --prefix frontend run build",
    "test": "python3 -m unittest discover -s tests -v"
  }
}
